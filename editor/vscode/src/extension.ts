import * as vscode from 'vscode';

export function activate(_context: vscode.ExtensionContext) {
	// Language configuration and grammar are declarative (package.json).
	// This activate function will host the LSP client in the future.
}

export function deactivate() {}
