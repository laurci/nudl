// FFI call benchmark — nudl calling C function via extern

extern {
    fn ffi_compute(x: i64) -> i64;
}

fn main() {
    let n = 50000000i64;
    let mut sum = 0i64;
    for i in 0i64..n {
        sum = sum + ffi_compute(i);
    }
    println(`{sum}`);
}
